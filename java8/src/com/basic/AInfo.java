package com.basic;

public class AInfo {

	// Function's method name  R apply(T)
	//Predicate's method name  boolean test(T)
	//Consumer's method name  void accept(T)
	// Supplier'method name   T get();
	
	// map(Function)
	//filter(Predicate)
	//forEach(Consumer)
	//generate(Supplier)
	//reduce(0,BinaryOperator)
	//anyMatch(Predicate)
	//AllMatch(Predicate) stream ko ak hi bar use kr sakte hai
}
